
- name: Create a directory if it does not exist
  ansible.builtin.file:
    path: '{{ dataDirPath }}'
    state: directory
    owner: '{{ dataDirOwner }}'
    group: '{{ dataDirGroup }}'
    mode: '0755'

